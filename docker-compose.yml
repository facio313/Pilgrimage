version: '3.8'

services:
  pilgrimageDB:
    build: ./db
    container_name: pilgrimageDB
    environment:
      POSTGRES_DB: pilgrimage
      POSTGRES_USER: pilgrimage
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pilgrimageDBData:/var/lib/postgresql/data
    networks:
      - pilgrimage
    restart: always

  pilgrimageRedis:
    image: redis:7-alpine
    container_name: pilgrimageRedis
    networks:
      - pilgrimage
    restart: always

  pilgrimageBackend:
    build: ./backend
    container_name: pilgrimageBackend
    depends_on:
      - pilgrimageDB
      - pilgrimageRedis
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.prod
      POSTGRES_HOST: pilgrimageDB
      POSTGRES_PORT: "5432"
      REDIS_URL: redis://pilgrimageRedis:6379/0
    command: >
      sh -c "python manage.py collectstatic --noinput &&
             python manage.py migrate --noinput &&
             gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 2"
    ports:
      - "9174:8000"
    networks:
      - pilgrimage
    restart: always

  pilgrimageFrontend:
    build:
      context: ./frontend
      args:
        VITE_KAKAO_JS_KEY: ${VITE_KAKAO_JS_KEY}
        VITE_API_BASE_URL: /api
    container_name: pilgrimageFrontend
    depends_on:
      - pilgrimageBackend
    ports:
      - "5174:80"
    volumes:
      - /home/cks/pilgrimage/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - pilgrimage
    restart: always

volumes:
  pilgrimageDBData:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/cks/pilgrimage/dbmnt

networks:
  pilgrimage:
    name: pilgrimage
    driver: bridge
